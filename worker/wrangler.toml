name = "one8-chat-bot"
main = "worker.js"
compatibility_date = "2024-11-11"

# Secrets are set with `wrangler secret put` (never stored here):
#   TELEGRAM_BOT_TOKEN, TELEGRAM_CHAT_ID, WEBHOOK_SECRET, STORE_DOMAIN
